#include <stdio.h>

int main() {
    // 여기에 코드를 작성해주세요.
    // 두 개의 정수를 입력받아 곱과 몫을 출력하는 프로그램을 작성해보세요.
    int a, b;
    scanf("%d %d", &a, &b);
    printf("%d * %d = %d\n%d / %d = %d", a, b, a*b, a, b, a/b);

    return 0;
}